mutation UpdateListingReview($id: ID!, $rating: Int!, $comment: String) {
  updateListingReview(id: $id, rating: $rating, comment: $comment) {
    id
    listingId
    userId
    rating
    comment
    createdAt
    user {
      id
      name
    }
  }
}
