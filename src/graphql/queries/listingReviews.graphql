query ListingReviews($listingId: ID!) {
  listingReviews(listingId: $listingId) {
    id
    listingId
    userId
    rating
    comment
    createdAt
    user {
      id
      name
      email
    }
  }
}
